#include <stdio.h>
#include <string.h>
int main() 
{
    char s[] = "banana", ch = 'a';
    int i, c = 0;
    for(i = 0; s[i]; i++)
        if(s[i] == ch) c++;
    printf("'%c' occurs %d times", ch, c);
    return 0;
}
