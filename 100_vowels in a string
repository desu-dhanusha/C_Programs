#include <stdio.h>
#include <string.h>
int main() 
{
    char s[] = "Dhanusha";
    int i, c = 0;
    for(i = 0; s[i]; i++)
        if(strchr("AEIOUaeiou", s[i])) c++;
    printf("Vowels = %d", c);
    return 0;
}
