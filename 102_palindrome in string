#include <stdio.h>
#include <string.h>
int main() 
{
    char s[] = "madam";
    char rev[50];
    strcpy(rev, s);
    strrev(rev);
    if(strcmp(s, rev) == 0)
        printf("Palindrome");
    else
        printf("Not Palindrome");
    return 0;
}
