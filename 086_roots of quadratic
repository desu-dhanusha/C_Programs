#include <stdio.h>
#include <math.h>
int main()
  {
    float a=1,b=-3,c=2,d=b*b-4*a*c;
    if(d>=0)
    {
        printf("Roots=%.2f %.2f\n",(-b+sqrt(d))/(2*a),(-b-sqrt(d))/(2*a));
    } else printf("Imaginary Roots\n");
    return 0;
  }
