#include <stdio.h>
#include <string.h>
int main() 
{
    char s[] = "I love C";
    int i, j=0;
    for(i = 0; s[i]; i++)
        if(s[i] != ' ')
            s[j++] = s[i];
    s[j] = '\0';
    printf("After removing spaces: %s", s);
    return 0;
}
